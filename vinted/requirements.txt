# Crawlee 0.6.12 currently relies on Pydantic 2.11's field handling and
# Browserforge 1.2.3's data-file API.  Later releases satisfy Crawlee's loose
# upstream ranges but fail while Crawlee is imported.
crawlee[playwright]==0.6.12
playwright==1.60.0
pydantic>=2.11,<2.12
browserforge==1.2.3
playwright-stealth==2.0.0
